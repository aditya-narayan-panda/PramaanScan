# ============================================================
# PramaanScan Backend - Production Requirements
# ============================================================

# --- Core Web Framework & Server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# --- Database & Models ---
SQLAlchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0
# Postgres driver -- required to point DATABASE_URL at Supabase/any Postgres in production.
# (Local dev can still use the default sqlite:/// URL, which needs no driver.)
# psycopg2-binary matches the plain "postgresql://..." URL Supabase gives you, unchanged.
psycopg2-binary>=2.9.9

# --- Authentication & Cryptography ---
PyJWT>=2.8.0
pwdlib[argon2]>=0.3.0
cryptography>=42.0.0
qrcode[pil]>=8.0

# --- Multimodal Media Analysis & Forensics ---
# The backend imports the existing PramaanScan_ML runtime models lazily.
# Keep the Python 3.12 guard because the bundled TensorFlow/MediaPipe video
# stack is intentionally targeted at Python < 3.13.
tensorflow>=2.15.0,<2.17.0; python_version < "3.13"
mediapipe>=0.10.0; python_version < "3.13"
imageio-ffmpeg>=0.4.9
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.4.0
joblib>=1.3.0
scikit-image>=0.22.0
Pillow>=10.0.0
pypdf>=4.0.0
librosa>=0.10.0
soundfile>=0.12.0
opencv-python-headless>=4.9.0
imageio>=2.30.0
# --- Testing & Quality Assurance ---
httpx>=0.27.0
pytest>=8.0.0
